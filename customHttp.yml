customHeaders:
  # This is here so that the jhbg.org site can load the jhorchestra.org RSS feed
  - pattern: /index.xml
    headers:
      - key: Access-Control-Allow-Origin
        value: https://www.jhbg.org
      - key: Access-Control-Allow-Methods
        value: GET
